'use client';

type RoomClientProps = {
  pitchId: number;
};

export function RoomClient({ pitchId }: RoomClientProps) {
  // TODO: garde ici tout ton code actuel de RoomClient
  // utilise pitchId comme avant

  return (
    <div>
      {/* Exemple minimal, remplace par ton vrai contenu */}
      <p>Room client pour le pitch #{pitchId}</p>
    </div>
  );
}





























